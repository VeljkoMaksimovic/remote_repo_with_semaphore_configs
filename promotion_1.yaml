version: v1.0
name: Promotion 1
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Promotion block
    task:
      jobs:
        - name: echo
          commands:
            - echo "this is a promotion"
